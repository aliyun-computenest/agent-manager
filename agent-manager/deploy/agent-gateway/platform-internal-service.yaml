apiVersion: v1
kind: Service
metadata:
  name: openclaw-platform-internal
spec:
  type: ClusterIP
  selector:
    app: openclaw-platform
  ports:
    - name: http
      port: 3001
      targetPort: 3001
